# 退潮赶海

6 处海岸、24 种发现、24 项探索目标；翻石、拨开海藻、局部挖沙、用网捞取、拖入小桶、逐件介绍和放生；每日样本、收藏与本地存档。

运行 `pnpm lint`、`pnpm test`、`pnpm build`，预览用 `pnpm preview`。构建输出 dist；`qa/package.py` 生成静态离线 ZIP，文件体积与散列写入 release/manifest.json。

当前完成范围、测试证据及未完成项见 [当前实现核对](CURRENT-IMPLEMENTATION.md)。详细设计仍保留为验收目标，不表示每项已经实现。
