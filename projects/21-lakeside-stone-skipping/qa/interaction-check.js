qaAction('QA 投掷',()=>{const app=document.getElementById('app');if(app.dataset.stage!=='ready')return;const x=innerWidth*.5,y=innerHeight>innerWidth?innerHeight*.55:innerHeight*.52;const ev=type=>app.dispatchEvent(new PointerEvent(type,{bubbles:true,pointerId:701,pointerType:'touch',button:0,buttons:type==='pointerup'?0:1,clientX:x,clientY:y}));ev('pointerdown');setTimeout(()=>ev('pointerup'),4200);});
