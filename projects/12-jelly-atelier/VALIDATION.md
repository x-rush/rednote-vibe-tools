# 制作玩法验证记录

日期：2026-09-06。环境：Codex 内置 Chromium，静态构建 `/dist/`。以下为桌面浏览器模拟视口验证，未冒充真实手机或小红书 App 内实测。

## 完整流程

- 花瓣、爱心、星星模具可选，三维轮廓确实变化；检查了双色爱心与双色星星的成品。
- 长按倒液后拖到按钮外松手：液量停在 `0.03674`，后续观察保持不变，按钮退出 pouring 状态。
- 第一层轻点加量上限为 `0.75`，此时长按入口禁用，可进入第二层；未到 20% 时换层入口禁用。
- 3 次底层加量与 7 次上层加量会准确进入装饰步骤，修复了浮点误差导致的 100% 假满。
- 点击花瓣和星星果冻顶面正中可以直接增加装饰；三种装饰均可通过辅助按钮摆放。
- 连续放置 9 次最多保留 8 颗；撤回后为 7 颗。装饰位置随配方保存，脱模后跟随果冻晃动。
- 完成普通成品与 7 颗树莓成品的脱模，能进入把玩状态；戳动按钮和直接触摸均可用。
- 将 30% 蜜桃乌龙 + 70% 橙光气泡的爱心作品收藏；刷新后从甜品柜重新打开，仍为相同模具、比例和 3 颗装饰。
- 旧版单口味收藏能迁移到等色双层配方。
- 捕获的浏览器 error/warn 日志为空。
- 最终构建另外走完了 40% 蜜桃乌龙 + 60% 紫葡萄雾的星星制作、三种装饰和脱模。截图：`artifacts/workshop-final-390.png`。

## 视口与安全区

通过开发服务器 `?safe-area=44` 注入非零顶部安全区。以下三个宽度均无横向或纵向溢出（初始制作页），品牌顶部高于模拟状态栏。

| 视口 | scrollWidth / scrollHeight | 品牌顶部 | 初始主按钮上 / 下边界 |
|---|---|---:|---|
| 375 × 812 | 375 / 812 | 62 | 698 / 751 |
| 390 × 844 | 390 / 844 | 62 | 730 / 783 |
| 430 × 932 | 430 / 932 | 62 | 818 / 871 |

- 375 × 812 + 44px 安全区的第一层操作区为 top=544.33、bottom=751，说明文案 bottom=240.93，按钮可见可用。
- 375px 甜品柜弹窗 top=212.04、bottom=599.96，处于安全区以下。
- 1440 × 1000 桌面检查 scrollWidth=1440，三维成品和文案区分别占据右侧、左侧；标题使用均衡换行，避免最后一个字单独成行。
- 浏览器视口覆盖恢复为默认，最终页面留在制作起点。

## 自动检查

最终在项目目录执行：

- `pnpm lint`：通过。检查 JS 语法、内容 ID 唯一性和运行时代码无远程 URL。
- `pnpm test`：**12 / 12 通过**。保留原有 5 个测试，增加倒液上限与时间步、精确满量、完整配方往返、异常配方过滤、旧收藏迁移、不同轮廓，以及三个模具正中射线命中的实际 Three.js 回归测试。
- `pnpm build`：通过。构建包含本地 Three.js 所需模块与许可证。

三维接缝回归修复的是极小三角函数余数造成的中心射线漏判；将近零轮廓坐标归零，保证视觉接缝和命中区域一致。倒液与脱模使用前台实际经过时间，避免帧率变化导致体验时间被拉长。以上不意味着做过真实移动设备的性能基准测试。

## 1.1.0 手绘模具验证（2026-09-06）

最终检查：`pnpm lint`、`pnpm test`（18 / 18）、`pnpm build`、`node scripts/package.mjs` 均通过。新增的 6 项测试覆盖首尾闭合规则、非法轮廓、正反向轮廓与平滑、凹形顶面与不同高度液面的实际 Three.js 射线命中、凹口外的装饰放置，以及精确的手绘配方往返存储。

浏览器实测：

- 390px 点选画出 C 形凹轮廓，三维预览出现相同凹口。确认后制作 40% 蜜桃 + 60% 葡萄果冻，放 2 颗装饰、脱模、戳动并收藏。
- 刷新后打开最新收藏，仍为手绘模具、40/60 配色和 2 颗装饰；确认恢复后的三维凹形画面。
- 在 4313 的受限 CSP 离线包中拖出未闭合的一笔，得到未闭合提示，确认入口禁用。
- 在同一包中点选四点交叉图，得到交叉提示，确认入口禁用；撤回一点后恢复有效预览。
- 选择修正后的模具、打开「修改轮廓」再取消，原选择保留。继续制作、装饰、脱模、保存、刷新重开均通过；甜品柜缩略图使用保存的轮廓 polygon。
- 最终受限包的 error / warn 日志为空。未使用网络、Worker 或新依赖。

画板弹窗的顶部 44px / 底部 20px 安全区检查：

| 视口宽度 | document.scrollWidth | 弹窗上 / 下边界 | 确认按钮下边界 |
|---|---:|---|---:|
| 375 × 812 | 375 | 67.50 / 744.50 | 727.83 |
| 390 × 844 | 390 | 83.15 / 760.84 | 744.18 |
| 430 × 932 | 430 | 136.15 / 795.84 | 779.18 |

以上为浏览器模拟，尚未在真实手机、小红书真机容器中测试。复杂轮廓会做平滑与尺寸归一化；不声称精确还原原始采样路径，也不支持内孔和自交。

发布包：`release/guodong-manman-1.1.0.zip`，656,882 字节。ZIP 根目录 index.html 正确，全部 5 个文件逐项 SHA-256 比较，与实际测试的 package 目录完全一致。完整包校验值见同名 .sha256 文件。
