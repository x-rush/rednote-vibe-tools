# 湖边打水漂 · 环境与交互验收

2026-09-20

## 本轮变更

湖面连续风浪法线、更集中的夕阳反光、远处细波；连跳水面尾迹；Canvas 后备同样有细碎动态反光。移除不协调的试验草片，保留自由拾石、循环蓄力与跟随镜头。

## 浏览器实测

实际点取扁圆青石，正常 PointerEvent 蓄力出手结算 10 跳 / 6.8m；同石再试与最佳纪录正常。强制无 WebGL 后有简化水面提示，844×390 准备状态 900ms 帧差 1.64%、平均差 0.98。控制台无错误。

375 / 390 / 430 CSS px、844×390 真实横屏布局、44px 顶部和 34px 底部模拟安全区及左右宿主占位层已检查实际场景、面板或暂停状态。无 CSS 模拟横屏功能。QA 辅助工具只存在 qa/ 和 .cache/environment-preview，不进入 dist / ZIP。帧差只证明指定区域在变化，不等同画质或设备性能评分；不同画幅数据不作性能对比。

## 自动检查与边界

项目目录 pnpm lint、pnpm test、pnpm build 全部通过，14 项测试。dist 和最终 ZIP 审计分别通过。未增加运行时依赖、后端或外网请求。

尚未完成小红书真机宿主、15 分钟持续运行帧率/发热与首次玩家易用性测试。浏览器模拟宿主覆盖层不能证明所有宿主版本无遮挡。画面是写实素材与局部实时动画，并非完整三维扫描或真实流体模拟；最终美术质量仍需用户验收。
