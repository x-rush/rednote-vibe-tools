# 八项目并行开发总规则

每个 Codex CLI 只允许修改它被分配的 projects/<project> 目录。不要修改其他项目、根 package.json、pnpm-workspace.yaml、pnpm-lock.yaml、docs/ 或 prep/。新增依赖先报告，由总控统一修改锁文件。

运行约束：纯前端静态构建；无后端、运行时 CDN 或必需外部 API；业务内容只放本项目 src/content/content.json；可用 localStorage / IndexedDB 保存结构化状态；禁止保存用户图片、Base64、音视频或 Blob；不依赖 Service Worker、Node API 或未经确认的设备 API。

提交前在项目目录执行 pnpm lint && pnpm test && pnpm build。至少验证 375 / 390 / 430 CSS px。不得删除测试来通过检查。所有 `sticky` / `fixed` 顶部控件及锚点跳转必须叠加 `var(--safe-area-inset-top, env(safe-area-inset-top, 0px))`，并以非零模拟安全区验证控件与目标内容均不被真机状态栏遮挡。

## 全项目共享记忆：小红书宿主按钮避让

2026-09-17 用户真机截图反馈：小红书宿主在屏幕左上方显示返回按钮、右上方显示操作按钮。这些按钮浮在工具页面之上，可能遮挡工具自己的按钮并导致误触退出。所有项目设计、修改及交付时都必须考虑这一点，不限于项目 15。

- 区分系统状态栏安全区与宿主按钮占位区。`env(safe-area-inset-top)` 不能代表宿主按钮区域；仅增加状态栏安全区不能解决宿主按钮遮挡。
- 宿主按钮的可见区域及周围触摸余量内，不得放置工具自己的按钮、菜单入口、关闭按钮、撤销、确认、横竖屏切换或其他交互热区。Logo、标题等重要信息也应避免被遮挡。不能通过提高页面 `z-index` 解决宿主层遮挡。
- 避让应以局部排布为主：将操作移到安全的中间区域、下方或浮动操作区。不要为了避让两侧按钮而在整个页面顶部堆叠大块空白；背景和非交互场景可以延伸到该区域。
- 同时考虑竖屏、设备真实横屏以及竖拿手机时通过 CSS 旋转的模拟横屏。模拟横屏只旋转工具内容，宿主按钮仍位于物理屏幕原来的位置；必须将宿主占位区映射到旋转后的布局，不能照搬竖屏顶部留白。横竖屏切换与抽屉关闭入口必须始终可见、可点且远离宿主返回按钮。
- 宿主按钮尺寸、偏移和屏幕截图像素不能直接当作固定 CSS 像素值，也不能假定不同设备及宿主版本完全一致。采用可配置的宿主避让参数；若没有已确认可用的宿主几何信息接口，使用保守布局并通过真机校准，不引入未经确认的设备 API。
- 交付验收除 375 / 390 / 430 CSS px 和非零系统安全区外，还须模拟左右宿主按钮覆盖层，覆盖首页、编辑、看效果、进入体验、弹窗和抽屉等实际存在的页面状态，并检查真实横屏与模拟横屏。普通浏览器预览没有宿主按钮，不能据此声称真机无遮挡；未完成宿主真机验收时须明确记录。
- 本节是主仓库共享约束，各项目沿用。新任务读取根 `AGENTS.md` 时必须纳入；已经运行中的其他任务需要重新载入本节，不应假定会自动刷新上下文。

## 全项目共享记忆：小红书音频打包与播放

2026-09-18 用户实际上传项目 17 时，平台拒绝 `assets/audio/incoming-ring.mp3`。上传端仅允许以下文件类型：`jpg、css、gif、svg、png、js、jpeg、json、html、woff2、webp、woff`。此上传结果优先于 skill 中“允许包内媒体”的笼统描述；不得因普通浏览器可播放 MP3 就把 MP3 直接放进交付 ZIP。所有项目使用音频时均遵循本节。

### 构建与包结构

- 原始 MP3 / M4A / WAV 等音频可保留在各自项目的开发素材目录，但不得直接进入上传 ZIP，也不得只改后缀伪装成图片或脚本。
- 参考项目 14 的方案：构建阶段读取音频字节，将其编码成 Base64，写入包内经典 JS 的静态音频数据；运行时还原字节交给 Web Audio 解码。不要通过 `fetch`、XHR、运行时 CDN 或外部 API 加载音频。
- 音频数据可输出为独立 `audio-data.js`，按依赖顺序先于播放器加载，也可由现有构建链合并进 `app.js`。产物不得依赖 `type="module"`、动态脚本或顶层 await。业务台词、文件映射与播放选项仍以本项目 `src/content/content.json` 为源；Base64 属于构建派生资源，不手工塞进业务内容文件。
- 每段 Base64 解码后的音频不得超过 1 MiB；超过 100 KiB 应记录风险并按需求裁剪、降低码率。不得用拆串、改后缀等方式绕过体积门禁。ZIP 不超过 10 MiB，建议不超过 2 MiB；同时评估全部音频解码后的内存，避免一次解码整套音频。
- 上文“禁止保存用户图片、Base64、音视频或 Blob”指用户运行期媒体不得持久化到 localStorage / IndexedDB 等草稿存储。本节允许的是经确认的内置素材在构建期生成静态 JS 音频资源；不允许借此持久化用户上传媒体。

### 运行时播放

- 检测 `window.AudioContext || window.webkitAudioContext`；在用户点击开始、接听等操作时创建或恢复音频上下文。缺少能力、恢复失败或解码失败时给出可理解提示，并保留无声操作路径。
- 使用 `atob` → `Uint8Array` → `AudioContext.decodeAudioData` 解码；需要兼容旧内核时使用回调接口并以 Promise 包装。通过 `AudioBufferSourceNode` 播放，用 `GainNode` 控制音量和静音，循环铃声设置 `source.loop = true`。
- 不把 Base64 拼成 `<audio src="data:...">`，也不以 Blob URL 作为媒体标签音源。这里使用的是字节解码后播放，不依赖容器禁止的 data/blob 媒体源。
- 每次播放创建新的 BufferSource；停止、拒接、挂断、取消延迟或离开页面时，停止节点、断开连接并取消计时器。使用会话标记防止异步解码完成后出现“已经取消却又响铃”。只缓存必要的解码结果，明确释放不再使用的资源。

### 校验与参考实现

- 打包前递归检查产物目录；压缩后再次检查 ZIP 每个文件的扩展名，严格按上述白名单校验，遇到不支持类型直接失败。确认 `index.html` 位于 ZIP 根目录，不能只检查 ZIP 大小。
- 按项目内 `.codex/SKILL.md` 的要求审计目录和最终 ZIP，并运行项目 lint / test / build。音频验证至少覆盖实际字节解码、开始播放、循环、静音、停止、延迟取消、解码期间取消及失败提示；替换播放链路后重新验证原有剧情或节奏逻辑。
- 浏览器验证与小红书真机验证分别记录。项目 17 此方案已通过浏览器解码与播放流程检查，但截至本次记录尚未完成新的 Web Audio 链路真机验收；不得据此宣称所有宿主版本都已验证。
- 参考构建：`projects/14-tongfu-rhythm/scripts/build-minitool.mjs`；参考音频适配：`projects/17-call-stories/src/audio-bank.mjs`；参考构建白名单：`projects/17-call-stories/tools.mjs`。只读参考其他项目，在自己被分配的项目内实现，不跨项目修改。
- 本节对仓库所有项目生效。已经运行中的 agent 需要重新读取根 `AGENTS.md`；文件更新不会自动刷新其上下文。
