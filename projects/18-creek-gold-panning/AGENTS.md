# 溪边淘金项目范围

仅允许修改 projects/18-creek-gold-panning/。继承仓库根 AGENTS.md 的所有约束，先重新读取，尤其宿主按钮避让与Web Audio音频打包。不要改其他项目、根依赖、锁文件、docs/或prep/。

实施先阅读 PROJECT.md 与 DELIVERY.md。用户已经要求本项目立项，当前文件是实施依据，不代表已有应用或验收完成。

新增依赖先报告，由总控统一修改。业务内容只放 src/content/content.json。最终应用检查须运行 pnpm lint、pnpm test、pnpm build 并完成ZIP目录和最终ZIP审计；规划阶段没有这些脚本，不得伪造通过记录。

可只读参考项目13/14/17，但禁止跨项目修改。未获得新的明确要求，不自行启动并行子代理或创建Codex任务。

