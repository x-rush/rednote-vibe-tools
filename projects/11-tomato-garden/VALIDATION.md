# 验证记录

2026-09-06，本地 Chrome 无头浏览器与 Node 内置测试。

- `pnpm lint && pnpm test && pnpm build` 全部通过，13 个单元测试通过。
- 1440px 桌面截图已人工查看。
- 375 / 390 / 430 CSS px：专注、小院、手记三个页面均无横向溢出。
- 三个手机宽度均设置 `--safe-area-inset-top: 44px`；页头和植物选择弹窗不进入模拟状态栏区域。
- 390px 全页截图已人工查看，开始按钮在 844px 高视口中可见。
- 浏览器端验证：开始、暂停、暂停期间不扣时、刷新保留暂停、继续、到点结算、刷新不重复结算、休息不奖励、提前结束保留整分钟、成熟收藏、已解锁品种切换。
- 验证含 HTML 特殊字符的任务标题正常作为文字显示。
- 本轮浏览器验证无 pageerror。
- 测试使用隔离浏览器与虚拟时钟，没有向用户预览浏览器写入测试进度。

截图与机器检查结果在被 git 忽略的 `artifacts/` 目录。复测浏览器可使用 `node scripts/browser-check.mjs <已安装的playwright模块绝对路径>`；此脚本使用系统 Chrome，测试工具不是产品运行依赖。

尚未在实体手机上测试锁屏唤醒；不保证后台或锁屏准点提醒。没有执行线上发布。
