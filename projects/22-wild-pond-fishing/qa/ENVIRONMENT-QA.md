# 野塘钓鱼 · 环境与交互验收

2026-09-20

## 本轮变更

六处钓位的水域内流动折射与细碎波光；岸边细芦苇和穗端随风；鱼体连续转向；抄网金属圈、网袋网格与杆柄渐变；收线/泄力提示可见，挑战文案不再覆盖张力警告。

## 浏览器实测

正常等待与 PointerEvent 操作完整经过 ready→cast→wait→nibble→bite→fight→net→caught，收获 8.9cm 鳑鲏、打开介绍并放流；无阶段注入或缩短等待。375 普通模式水域 900ms 帧差 8.76%、平均差 3.38；430 减弱动画模式 1.65%、平均差 1.41。完整流程在最后网格外观修饰前完成，最终模型未改。

375 / 390 / 430 CSS px、844×390 真实横屏布局、44px 顶部和 34px 底部模拟安全区及左右宿主占位层已检查实际场景、面板或暂停状态。无 CSS 模拟横屏功能。QA 辅助工具只存在 qa/ 和 .cache/environment-preview，不进入 dist / ZIP。帧差只证明指定区域在变化，不等同画质或设备性能评分；不同画幅数据不作性能对比。

## 自动检查与边界

项目目录 pnpm lint、pnpm test、pnpm build 全部通过，40 项测试。dist 和最终 ZIP 审计分别通过。未增加运行时依赖、后端或外网请求。

尚未完成小红书真机宿主、15 分钟持续运行帧率/发热与首次玩家易用性测试。浏览器模拟宿主覆盖层不能证明所有宿主版本无遮挡。画面是写实素材与局部实时动画，并非完整三维扫描或真实流体模拟；最终美术质量仍需用户验收。
