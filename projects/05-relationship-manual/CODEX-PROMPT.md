# 新 Codex 会话提示词

只在 `projects/05-relationship-manual` 工作；本会话代表用户明确恢复暂停项目。先读根/项目 `AGENTS.md`、`TODO.md`、`PROJECT_BRIEF.md`、`DESIGN.md`、`IMPLEMENTATION_PLAN.md`、`UX-SPEC.md`、`UI-HANDOFF.md`、`NPC-SPEC.md` 和 `VISUAL-QA.md`。

保留已有改动，不改其他项目、根配置、锁文件或总控文档。产品不是心理诊断工具，不分析上传的聊天记录；业务内容仅在 `src/content/content.json`，本地只存结构化草稿，不保存图片/Blob。按 TODO 实现一个完整切片并补安全边界测试。运行 `pnpm lint && pnpm test && pnpm build`，验证 375 / 390 / 430 CSS px，只提交本目录，报告提交号、剩余项、隐私检查和美术需求。
