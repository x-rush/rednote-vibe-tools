# 新 Codex 会话提示词

只在 `projects/06-departure-checker` 工作；本会话代表用户明确恢复暂停项目。先读根/项目 `AGENTS.md`、`TODO.md`、`PROJECT_BRIEF.md`、`DESIGN.md`、`IMPLEMENTATION_PLAN.md`、`UX-SPEC.md`、`UI-HANDOFF.md`、`NPC-SPEC.md` 和 `VISUAL-QA.md`。

不得改其他目录、根配置、锁文件或总控文档。清单必须由本地 JSON 和确定规则生成，不调用天气或地图 API；不提供医疗或安全保证。按 TODO 完成一个端到端场景并补规则测试。运行 `pnpm lint && pnpm test && pnpm build`，验证 375 / 390 / 430 CSS px，只提交本目录，报告提交号、规则覆盖率、剩余 TODO 和美术缺口。
