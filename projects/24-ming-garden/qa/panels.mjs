import {createRequire} from 'node:module';import assert from 'node:assert/strict';const require=createRequire(import.meta.url),{chromium}=require('C:/Users/77958/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const b=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});for(const [width,height] of [[390,844],[844,390]]){const p=await b.newPage({viewport:{width,height}});await p.goto('http://127.0.0.1:4327/');const btn=p.locator('.canvas-tools [data-action="panelToggle"]');assert.equal(await btn.innerText(),'收起');await btn.click();assert.equal(await btn.innerText(),'展开');assert.equal(await p.locator('.app-shell').getAttribute('class').then(x=>x.includes('panels-collapsed')),true);assert.equal(await p.locator('.camera-bar').evaluate(e=>getComputedStyle(e).display),'none');await btn.click();assert.equal(await btn.innerText(),'收起');await p.close();}await b.close();console.log('PASS panels collapse/expand in portrait and landscape');
