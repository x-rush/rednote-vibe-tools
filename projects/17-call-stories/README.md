# 捡手机文学局

来电聊天朋友圈，轻松写故事。纯前端静态工具，无必需外部 API。

## 当前功能

- **人物库**：默认 16 位人物，可新建、改名、修改关系、选择 24 张同系列手绘头像或临时上传自己的头像。来电人、聊天对象、右侧角色、消息发送者、朋友圈发布者、点赞人及评论人都引用人物 ID，改名或换头像同步生效。新建人物从 32 个虚构昵称中选择未使用名字，优先分配未使用头像；编辑中可点“换个昵称”。最多 60 人；正在使用的人物需先解除引用才能删除。
- **来电与通话**：六套固定录音脚本；支持立即或延迟来电、接听、拒绝、计时、声音开关、任意时刻挂断、只播开场或完整通话。播放结束后再计算句间停顿；脚本可自动结束或保持通话。台词默认隐藏，可在设置中显示。来电铃声默认开启，支持在更多设置中关闭；延迟期间静音，到时响铃，接听、拒绝和退出时停止。不采集麦克风。拒绝、挂断或对方结束后直接返回来电人的聊天框，追加拒接或通话时长记录；切换来电人时保留其他人的聊天草稿。
- **聊天**：支持一对一私聊与多人群聊，分别保留草稿。群聊可编辑群名、勾选成员，顶部显示人数、左侧消息显示发送者昵称；私聊仅两位角色且不显示消息昵称。通话结束只追加到私聊。选择左右角色，每条消息可指定人物、编辑、删除、排序。多人发言时显示名字；自定义标题、以独立下拉选择星期或月日，以及小时、分钟；时间格式为“周几＋时间”或“月日＋时间”，统一头像、气泡、输入栏图标。
- **朋友圈**：选择发布者，编辑正文和时间，临时添加封面和最多九张照片；点赞支持人物多选，评论逐条指定人物，支持随机填充。预览采用文字点赞与评论布局。

## 保存图片与打包

聊天和朋友圈预览底部可保存图片。原生容器通过 `window.xhs.miniTool.saveImageToPhotosAlbum({filePath})` 保存 PNG；普通浏览器展示图片预览，可在电脑上右键另存为。图片不进入浏览器存储。以 390 CSS px 排版、780 px 输出，完整内容超长时按段落或消息边界分张，每张高度最多 3600 px。无网络截图依赖。

Logo 为原创手机与对话气泡图形，矢量文件 `assets/logo.svg`，512 px PNG 文件 `assets/logo.png`。简介（含逗号共 13 字符）：来电聊天朋友圈，轻松写故事。

依照仓库 `.codex/SKILL.md`（minitool-zip-builder 1.6.0）构建：复用工作区已有 Rolldown 转译至 Chrome 61，生成经典 `app.js`，内容在构建期打入，不再 fetch JSON；参考项目 14，音频在构建时编码为 JS 数据，由 Web Audio decodeAudioData 解码；ZIP 不再包含 MP3 文件，不使用 data/blob 媒体源。每段原始音频限制 1 MiB，当前最大剧情片段 54,203 字节，铃声 25,513 字节。当前 ZIP 为 689,281 字节，低于建议的 2 MiB。构建对上传文件后缀执行白名单检查。ZIP 根目录包含 index.html。构建无新增依赖。

## 配音制作

直接使用 [AUDIO-PROMPTS.md](AUDIO-PROMPTS.md)：六套豆包 TTS 2.0 人物音色、16 段朗读文本、准确文件名和停顿节奏。六套配音及来电铃声均已内置；原始音频保存在 `assets/audio/doubao-source/`，分段音频在 `assets/audio/`。生成时保留自然语速，句间等待由程序控制。挂断后释放解码缓存；播放失败会提示并保留无声体验。

## 本地运行

项目目录执行 `pnpm dev`；预览地址 `http://127.0.0.1:4317`。
交付检查：`pnpm lint`、`pnpm test`、`pnpm build`。
浏览器验证：构建后启动预览，再执行 `node qa/browser-check.mjs`（需要本机 Chrome 与 Node 24）。

## 文件与存储

- `src/app.mjs` 为当前页面入口，`src/model.mjs` 负责共享人物数据，`src/call-player.mjs` 负责录音播放调度。
- 业务内容、配音提示词和台词均在 `src/content/content.json`；`AUDIO-PROMPTS.md` 是对应的人类可读制作单。
- localStorage 仅保存人物 ID、姓名、内置头像 ID、文本及选项。所有上传照片只用内存中的 object URL，不保存 Base64、图片、音频或 Blob，刷新后恢复内置头像，动态照片需重新选择。
- 没有真实微信连接、消息发送或朋友圈发布。

## 适配记录

已验证 375 / 390 / 430 CSS px，24px 非零安全区，左右宿主按钮模拟覆盖层；另验证 844 × 390 横屏。截图和结构化结果在 `qa/output/`。页面没有 CSS 旋转的模拟横屏入口。已验证浏览器中 PNG 生成、含临时照片的朋友圈、45 条长聊天分张、JSBridge 参数与拒绝授权分支。尚未完成小红书真机相册保存和播放验收；Chrome 61 JS/CSS 兼容性未实测，真机性能未实测。


## 界面更新（2026-09-18）

创作首页增加当前来电人物、场景和直接开始入口；暖灰背景、白色编辑卡片、深绿主操作统一层次。人物库改为双列头像卡片，消息编辑按自己/对方区分背景；朋友圈照片与封面并排，点赞与评论分组。周几、小时、分钟并排，顶部安全区与原有紧凑高度保持。新增视觉仅作用于创作编辑区域，聊天、通话、朋友圈效果页沿用用户参考样式。

已检查新版首页、来电编辑、聊天编辑、朋友圈编辑、人物库、人物选择与编辑截图。主页直接开始来电可用；21 项测试及 375/390/430、24px 安全区、模拟宿主覆盖层、横屏、导出回归通过。当时上传 ZIP 689,281 字节，目录/ZIP 审计及扩展名白名单通过。
## 朋友圈操作修复（2026-09-18）

更多按钮改为固定尺寸的双点 SVG，展开深灰色赞/评论菜单；点赞支持取消，评论可从人物库选择身份，支持发送、取消及空内容校验。编辑区的评论人物与删除按钮并排，正文独立排列。保存图片排除临时菜单和输入区域。新增三个宽度的菜单边界、评论发送与取消、图片保存回归；来电、通话、单聊和群聊原有回归全部保留。真机验收状态同上。

本次最终包 690,490 字节；目录/ZIP 审计零警告、扩展名白名单通过。朋友圈滚动时导航固定于宿主安全区下方；导出恢复静态导航。

## 朋友圈时间与评论人物选择（2026-09-18）

发布时间支持刚刚、几分钟前、几小时前、几天前、昨天+时间、周几+时间、月日+时间，全部使用下拉选择。月日不含年份，二月允许选择29日。评论人物改为头像与昵称列表，切换身份保留输入草稿。22项测试通过；375/390/430、非零安全区及核心浏览器回归通过。最终ZIP白名单及双审计通过，真机验收仍待完成。
