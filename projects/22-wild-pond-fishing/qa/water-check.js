// Development-only water animation measurement; never bundled into the game.
const waterButton=document.createElement('button');waterButton.textContent='QA 水面帧差';waterButton.style.cssText='position:fixed;right:8px;bottom:160px;z-index:50';waterButton.onclick=async()=>{const canvas=document.getElementById('scene'),ctx=canvas.getContext('2d'),x=Math.round(canvas.width*.46),y=Math.round(canvas.height*.4),w=Math.round(canvas.width*.2),h=Math.round(canvas.height*.08),a=ctx.getImageData(x,y,w,h).data;await new Promise(r=>setTimeout(r,850));const b=ctx.getImageData(x,y,w,h).data;let n=0;for(let i=0;i<a.length;i+=4)if(Math.abs(a[i]-b[i])+Math.abs(a[i+1]-b[i+1])+Math.abs(a[i+2]-b[i+2])>8)n++;waterButton.textContent='水面变化 '+Math.round(n/(w*h)*100)+'%';};document.body.appendChild(waterButton);
