# 我希望被这样对待 · 视觉与交互审查

> 审查日期：2026-08-24  
> 审查对象：`design/preview.html`（12 个关键界面）  
> 历史结论：当时的纯排版方向通过。用户随后确认小满陪伴型 NPC，最终 V2 结论见文末；情侣和关系场景位图仍不生成。

## 覆盖状态

- 首次封面与本地隐私提示
- 草稿存在、继续整理、查看现稿和重新开始入口
- 非诊断、不代发、非服从清单的使用边界
- 分章问答、两端体面答案和“暂不确定”
- 已完成计算后的真实整理过场
- 七章说明书总览和敏感项标记
- 系统建议稿、用户编辑稿、隐藏与敏感设置
- 答案改变后的编辑冲突和三种明确处理
- 拖拽排序与键盘上下移动
- 完整版预览
- 默认排除敏感细节的简洁版预览
- 存储失败后的临时会话、截图和纯文字备份

## 评分

| 维度 | 当前评分 | 结论 |
|---|---:|---|
| 信息架构 | 9.3/10 | 问答、生成、编辑、排序和分享版本职责清楚。 |
| 隐私与边界 | 9.5/10 | 本地、非诊断、不代发和非服从清单均在主流程可见。 |
| 编辑安全性 | 9.4/10 | 用户文字不会被答案变化静默覆盖。 |
| 品牌独特性 | 9.2/10 | 成熟信件、编辑批注和克制色彩足以形成辨识度。 |
| 包容性 | 9.3/10 | 不使用情侣图、性别预设或恋爱测试语言。 |
| 响应式成熟度 | 8.2/10 | 390px 基准通过；软键盘、长编辑稿和 375/430px 待验证。 |
| 美术必要性控制 | 9.5/10 | 已证明无需 AI 人物位图，避免无功能价值的资源膨胀。 |

## 本轮通过的关键决定

1. 产品定位是“整理相处偏好”，不是关系评分、依恋诊断或人格测试。
2. `仅保存在本设备` 始终可见，但不用危险红色制造恐慌。
3. 结果不会自动发送；截图和分享版本都必须由用户主动触发。
4. 每屏只处理一个关系主题，选项两端都使用体面、中性表达。
5. 允许选择“暂不确定”，不强迫用户制造确定性。
6. 生成过场发生前内容已经计算并保存，动画只是整理表现，不是假加载条。
7. 七章结构固定为陪伴、安慰、分歧、边界、关心、建议、我也会努力。
8. 用户手工编辑与系统建议分开保存；答案变化时标记“需要确认”。
9. 冲突处理提供保留编辑、采用新建议、查看逐句差异，绝不自动覆盖。
10. 拖拽过程中不连续写 IndexedDB，只在放下或按下移动按钮后保存。
11. 简洁版明确声明不包含全部内容，默认排除标记为敏感的边界细节。
12. 存储失败时仍可完成当前会话、生成截图和复制纯文字，但明确关闭后不保留。

## 美术资源冻结结论

第二轮完整页面证明：**不需要封面人物位图，不需要情侣插画，不需要关系场景图。**

原因：

- 人物图不能帮助用户理解任何功能或状态；
- 会把“任何重要关系”误导为恋爱关系；
- 容易引入年龄、性别、性取向和家庭结构刻板；
- 会压缩本已成立的信件排版品牌；
- 增加包体、加载失败和分享卡裁切风险。

正式资源范围冻结为：

| 资源 | 数量 | 形式 | 用途 |
|---|---:|---|---|
| 七章节符号 | 7 | 项目内 SVG | 总览、排序、编辑和分享版本 |
| 本地／隐私符号 | 1 | SVG | 克制的本地状态标记 |
| 敏感内容符号 | 1 | SVG | 不依赖红色表达敏感状态 |
| 编辑冲突符号 | 1 | SVG | `需要确认` 状态 |
| 纸纹 | 2 | CSS 或小尺寸无缝纹理 | 主信纸与批注纸 |
| 折痕／页角 | 1 套 | CSS/SVG | 轻度纸张层次，不影响正文 |
| 批注线与修订标记 | 1 套 | SVG/CSS | 系统建议、用户编辑、差异查看 |
| 完整版截图模板 | 1 | HTML/CSS | 可多页或长图导出 |
| 简洁版截图模板 | 1 | HTML/CSS | 4:5 或手机长图，排除敏感项 |

所有图形均可在代码仓库内直接制作，不需要 AI 生图，也不存在现实原型考据负担。

## 下一轮验收

- 制作七个语义统一但不幼态的章节 SVG，并检查 16/24/32px 可辨识度。
- 完成完整／简洁截图模板的实际 Canvas 或 DOM 导出测试。
- 在 375×812、390×844、430×932 下检查七章列表、长句换行和底部安全区。
- 打开移动软键盘测试编辑页，按钮不能遮挡输入和字数统计。
- 使用 0、1、7 个可见章节验证空白分享、极短说明书和完整长文。
- 使用 180 字上限、连续英文、数字、emoji 和换行测试编辑排版与安全转义。
- 验证拖拽与键盘排序都只写入一次，并在失败时恢复原顺序。
- 验证简洁版不会因排序、隐藏或冲突状态意外包含敏感章节。
- 测试存储失败时截图和纯文字复制是否仍可使用。


## 全局三宽横向溢出回归（2026-08-24）

- 本项目参与 375／390／430px Playwright 回归：12 状态、无位图依赖。
- 修复单列 CSS Grid 的最小内容宽度后，三宽 document.scrollWidth === viewportWidth，全部手机外框横向越界为 0，当前 <img> 断图为 0。
- 本项只证明横向边界与静态图片加载；逐屏纵向内容、动态长文案和 WSL 真实应用仍须继续验证。统一记录见 docs/00-总控与共用/38-八项目375-390-430响应式回归记录.md。
# 我希望被这样对待 · 第二轮视觉与交互审查

## 引导角色小满 · 单资源与首次装回验收（2026-08-24）

- 事实参考：Pexels 7034454 用于暖灰针织材质、书写手势与自然光；Pexels 5257772 用于共同整理纸面内容的工作关系。两张照片只作现实锚点，不复制真人。
- 边界通过：小满呈清爽中性编辑搭档气质，不是治疗师或恋爱对象；无白大褂、听诊器、诊所、情侣、爱心、粉色浪漫渐变或诊断量表。
- 单资源通过：暖灰开衫、鼠尾草绿衬衣、蓝铅笔、恰好两张空白卡片和双手结构可信；背景没有可读文字和私密内容。
- 装回返工：V1 首次叠层时对话卡遮住第二张卡片；调整 `.guide-stage` 与主图独立高度后，两张卡片和铅笔完整可辨，未破坏自然构图。
- 实际 UI：人物不覆盖“仅保存在本设备”、角色身份、核心边界文案或操作；不在用户私密编辑页常驻大立绘。
- 三宽实测：375／390／430px 的 document 宽度等于 viewport；0 断图；900×1200 主资源正常解码；对话卡完整在手机框内；双按钮高度均 48px。
- 性能与降级：三份 WebP 合计 124,712 bytes；首次引导只加载主图与小头像，占位图 1,166 bytes。图片失败时角色名、身份、隐私声明、跳过和继续按钮仍完整。
- 四类状态已接入：首次引导、选择冲突节点反馈、编辑页召回帮助、本地保存失败恢复。后三类各有关闭或继续路径，帮助层关闭后明确保留原句与光标位置，恢复层优先复制／截图而非重做。
- 375／390／430px 共 9 组后三态复测：0 断图、document 无横向溢出，所有按钮完整位于手机框内，最小触控高度 48px。

## 小满与内容 V2 最终验收（2026-08-26）

覆盖流程：`首页 → 三种关系语境 → 7 次章节转场 → 21 题 → 冲突提示 → 21 题回顾 → 七章结果 → 编辑 → 简洁版 → 本机保存 → 刷新恢复`。

| 视口与语境 | 横向边界 | 图片 | 最小按钮 | 流程结果 |
|---|---:|---:|---:|---|
| 375×812 · 亲密关系 | 375 = 375 | 0 断图 | ≥44px | 完整流程、编辑、简洁版、保存恢复通过 |
| 390×844 · 好友关系 | 390 = 390 | 0 断图 | 44px | 完整流程、编辑、简洁版、保存恢复通过 |
| 430×932 · 家人关系 | 430 = 430 | 0 断图 | 44px | 7 转场、21 题、3 多选、编辑、保存恢复通过 |

关键结论：

- 375px 题目页初检发现场景引语与右侧批注重叠；将窄屏批注改为正常文档流后复检通过。
- 最终卡片固定生成 7 章；简洁版在默认隐藏敏感章节时显示 5 章，且仍保留“我的需要／可以这样做”等角色标签。
- 三张 1080×1440 WebP 均正常解码；总计 784,032 bytes，透明通道有效。
- 拦截 `xiaoman-daily-v2.webp` 后，立绘替换为“满”字印章；姓名、角色身份、完整台词和页面操作仍保留，页面无横向溢出。
- `prefers-reduced-motion: reduce` 时，立绘 `animation-name: none`、过渡为 `0s`；文件夹和对话框不发生位移。
- 模拟读取 localStorage 抛出 `SecurityError`：不再进入阻塞错误页；首页、3 个语境和当前临时会话可继续，小满显示重试保存／继续当前会话。
- V2 envelope 约 16 KB；不包含图片、Base64、Blob URL、NPC 对话历史或用户媒体。
- 干净正常会话控制台 0 error、0 warning；故意拦截图片时产生的网络错误只属于降级测试。

## 首页立绘主视觉调整（2026-08-26）

- 移除首页深色框体和硬边托底；透明立绘直接进入暖白信纸场景，以低对比纸色光晕建立层次。
- 桌面端采用左侧标题／操作、右侧大幅小满的双栏海报；移动端阅读顺序改为标题、小满、对话纸签、隐私与操作。
- 375／390／430px 立绘可见宽度分别约为 348／363／403px，脸部与上半身均进入首屏。
- 三宽均满足 `scrollWidth === clientWidth`、0 断图，首页按钮高度约 47.2px。
- 1280×900 下页面无额外纵向滚动，小满以约 577px 宽立绘占据右侧主视觉。
- 减弱动效保持 `animation-name: none`、过渡 `0s`；拦截首页立绘后仍显示“满”字印章、对话和开始按钮。

## NPC 全流程尺寸分级（2026-08-26）

- `hero`：仅首页使用，移动端立绘约 348–403px 宽，桌面作为右侧主视觉。
- `stage`：关系选择与章节转场使用。第二页改为浅色开放舞台；375／390／430px 的角色舞台高度约为 304／304／335px，桌面约 486px。
- `avatar`：连续答题与存储提醒维持约 52px；结果装订提升至 64px；冲突提示提升至 64–72px，移动端不再隐藏角色。
- 第二页移动端阅读顺序为标题 → 小满 → 对话 → 关系选项 → 使用原则；375px 第一项顶部约 738px，首屏可以看到操作入口。
- 章节页保留大立绘，但答题页立即收回小头像，避免持续挤压题目和选项。
- 首页、第二页与章节页均无黑色托底；对话纸签避开脸部和双手。
- 375／390／430／1280px 均无横向溢出、0 断图、最小按钮 44px；正常会话控制台 0 error、0 warning。
- `hero` 与 `stage` 图片请求被拦截时均显示“满”字印章，第二页三个关系选项保持可用；减弱动效仍为 `animation-name: none`、过渡 `0s`。

## 答题与结果头部密度优化（2026-08-26）

- 移除答题页与结果页独立占行的 NPC 陪伴条，将陪伴信息并入当前内容的标题区域。
- 每章首题在题卡头部显示 40px 小满头像与最多两行章节提示；375px 下整段提示高 49px，题卡头部总高 119px。
- 同章后续题只保留 38px 安静头像；375px 下题卡头部降至 62px，不再重复台词，也不会把题干继续向下推。
- 结果页将小满改为标题区内的编辑签名；375／390／430px 下签名条均高 75px，标题区到说明书工作区保持 20px 常规间距，不再存在独立绿色占位带。
- 375／390／430px 复检均为 0 横向溢出、0 断图、最小按钮 44px；正常流程控制台 0 error、0 warning。

## 手机 PNG 分享卡导出（2026-08-26）

- 结果页增加“保存简洁长图”；点击后同步切换到简洁预览，并完全在本地生成图片，不上传内容。
- 保存能力遵循项目根目录 `.codex/SKILL.md`：Canvas 生成完整 PNG Data URI 后，依次调用 `window.xhs.miniTool.writeTempFile({ data })` 与 `window.xhs.miniTool.saveImageToPhotosAlbum({ filePath })`。源码不再包含 Blob、Object URL、`<a download>` 或浏览器下载兜底。
- 图片保持 1080px 宽、最小 1350px 高，并根据所选简洁版段落自动增高。测试场景实际生成 1080×1476 PNG，文件头、尺寸与桥接参数校验通过，所选段落无省略号截断。
- 分享卡包含关系语境、摘要、当前简洁版可见章节、表达角色、免责声明与“小满整理”印章；敏感章节继续服从简洁版开关。
- 首轮导出发现章节正文超出纸张右边界；根因是 27px 正文按每行 28 个全角字符切分，超过 640px 可用栏宽。改为每行最多 22 个字符并增加回归测试后，视觉检查无裁切。
- 修复前简洁预览在 900px 为 `clientHeight 569 / scrollHeight 1081`、1280px 为 `638 / 1078`，固定 4:5 比例与 `overflow: hidden` 会截掉下半部分。移除固定比例后，375／390／430／900／1280px 的 `clientHeight` 均等于 `scrollHeight`，页脚完整位于卡片内部，页面横向溢出均为 0。
- 使用与文档契约一致的模拟 JSBridge 验证保存顺序、参数与成功提示；没有触发浏览器下载事件，控制台 0 error、0 warning。真机仍需由小红书客户端处理相册授权弹窗。
- PNG Data URI 只用于当次写入临时文件，不写入 localStorage／IndexedDB；“保留在本设备”仍只保存可编辑的结构化结果。

## 答题操作栏布局修复（2026-08-26）

- 根因：操作栏固定预留三列，但多数题只有“上一题”和主操作两个按钮；窄屏又把主操作强制到第一行，造成按钮错行与半宽孤立。
- 普通题改为两列自适应布局，上一题／主操作保持同一行，主操作权重更高；900px 下完整利用 828px 操作栏，不再遗留约 378px 空列。
- 普通可跳过题在手机端将“上一题／暂不确定”作为两个等宽次级操作同排，“收好这张，继续”独占下一行；桌面端仍保持三按钮同排，主操作最宽。
- 375／390／430px 实测两个次级按钮宽度分别为 162／169／189px，主按钮宽度分别为 333／348／388px；900px 三按钮宽度为 212／212／370px。按钮高度均为 47px，0 横向溢出、0 控件文字裁切，控制台 0 error、0 warning。

## 单题修改直接返回总览（2026-08-26）

- 从答案总览进入任意题目后，操作栏增加“保存修改并返回总览”；原有“收好这张，继续”保留，仍可用于顺序检查后续答案。
- 直接返回只结束本次改单题状态，不清空其他答案、不提前重新生成说明书，也不把临时导航状态写进草稿。
- 浏览器实测在第 9 题新增第二个选项后直接返回，总览立即显示两项新答案，其余 20 题答案保持原样。
- 含“上一题／暂不确定／收好这张，继续／保存修改并返回总览”四个操作时，375／390／430／900px 均为 0 横向溢出、0 文字裁切，所有按钮高 47px；手机端返回按钮独占一行，避免再次出现窄屏错位。

## V3 独立题库回归

- [x] 375 × 812：亲密／好友／家人各完成一轮关键页面检查
- [x] 390 × 844：亲密／好友／家人各完成一轮关键页面检查
- [x] 430 × 932：亲密／好友／家人各完成一轮关键页面检查
- [x] 三套题目、复核与结果无关系专属措辞串库
- [x] 修改答案只定位当前关系题库，不要求重答全部问题
- [x] V2 草稿迁移提示准确，等价答案未被静默清除
- [x] 简洁卡片页面与导出图片布局一致

实测证据（2026-08-26）：

- 三种关系均为独立 21 题、每章 3 题；九种“语境 × 视口”组合均完成章节转场、答题、21 项总览、结果和简洁版，共覆盖 189 次题目交互。
- 九种组合均满足 `document.documentElement.scrollWidth === innerWidth`；375 px 单题修改页的四个操作互不重叠，直接返回后其余 20 道答案保持不变。
- 亲密关系首章提示、好友关系“新圈子／群体误会”、家人关系“家庭争执／家人道歉”等专属场景仅出现在对应题库；好友与家人结果未出现“亲密接触／分手”等串库词。
- 375 px 简洁卡宽 333 px，390 px 为 353 px，430 px 为 392 px；页面折角未覆盖标题或右上角版本文字。导出 SVG 的关系标签、章节标签、角色层级与单列页脚由回归测试逐项对齐页面卡片。
- 模拟 V2 好友草稿后，提示为“已保留 1 道旧答案，另有 20 道需要重新确认。”；原“稍后回复时间”答案准确迁移到好友第 2 题，并在 3.8 秒检查时确认提示已自动关闭。
- 三档正常流程控制台均为 0 error、0 warning；相册导出仍严格使用 `writeTempFile({ data: 完整 PNG Data URI })` → `saveImageToPhotosAlbum({ filePath })`，不写入图片、Base64 或 Blob 到本地草稿。
