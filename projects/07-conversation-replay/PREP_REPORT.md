# 《当时这样说就好了》FOUNDATION 准备报告

日期：2026-08-24  
项目：`conversation-replay`  
内容版本：`1.0.0`  
存储 Schema：`1`

> V2 更新（2026-08-26）：当前实际应用使用 schema `2` 和“事实—感受—推测—需要—请求”五层编辑台。本报告下方的 V1 架构说明保留作迁移审计；最终实现与验证以本节及 `VISUAL-QA.md` 的 V2 记录为准。

## V4 首页 Hero 与开始前确认页交付摘要（2026-08-26）

- 新增无状态 `LandingHero` 与 `PrivacyStage`：首页成为修订纸条、迟言完整立绘和署名批注组成的编辑手账封面；开始前确认页成为三条隐私保证与两种真实模式按钮组成的陪伴工作台。
- 首页与确认页不再渲染通用陪伴横卡；关系、目标、情境页继续使用 featured 陪伴条，五层编辑及后续页面继续使用紧凑陪伴条。
- 未新增依赖、美术资源、存储字段或业务分支；继续复用已验收的 `chiyan-guide-master.webp` 和本地占位图，两种保存模式仍派发现有 reducer action。
- 新增 2 个组件测试文件，并补充入口内容契约及空白文案拒绝测试；自动门禁更新为 16 个测试文件、106 项测试。lint、test、TypeScript 与 Vite 生产构建通过。
- 浏览器覆盖 375×812、390×844、430×932、1024×900 与 32px 非零安全区；三宽 0 横向溢出、0 断图、最小按钮 48px、控制台 0 error／warning。
- 两个舞台图片失败时切换到本地占位图且操作区位移为 0；减少动态模式下无动画和 transform。生产构建产物为 CSS 22.22 kB（gzip 5.12 kB）、JS 388.84 kB（gzip 106.25 kB）。
- 首页与确认页的标题、按钮、修订示例、隐私保证和模式说明全部收口至 `content.json` 的 `content.intro`，由类型与运行时校验共同保护；375×812 加 32px 安全区时主按钮顶部为 797px。

### “问迟言”头像召回层补充

- 顶栏帮助入口由通用 SVG 改为迟言圆形头像：复用完整主立绘，通过 32px 裁切视窗与 CSS 位移重新取景，移除原头像左侧留白并保留完整正脸；移动端只隐藏“问迟言”文字，桌面同时显示头像与文字。
- 帮助弹层消费当前 `CompanionViewModel`，以当前页面姿态的大幅立绘和 invitation／reassurance／autonomy 组成纸张式对话框；不新增硬编码 NPC 台词。
- 弹层继续提供当前步骤、句式提示、可展开的陪伴边界、安全退出和返回操作；返回按钮固定在弹层底部。
- 新增 2 个 SSR 组件测试；自动门禁更新为 18 个测试文件、108 项测试。生产构建产物为 CSS 25.11 kB（gzip 5.64 kB）、JS 389.55 kB（gzip 106.38 kB）。

## V3 迟言陪伴交付摘要（2026-08-26）

- 迟言从首次引导角色升级为贯穿 19 个页面状态的“温和编辑搭档”；每个时刻都从内容包提供邀请、自主权提示和语义姿态，React 不硬编码 NPC 台词。
- 新增 attend、observe、sort、pause、compose、complete 六张 900×1200 WebP；连同欢迎主图形成 7 个可见姿态，safety 克制复用 pause。项目资源现为 9 个 WebP、17 个 SVG，全部本地静态加载。
- 完成 32 个情境、948 个正向文案字段和 19 个 NPC 时刻的复核；情境冻结事实无需改动，安全提示补强“现实中的可信任者”优先级。逐项结论见 `COPY-AUDIT-V3.md`。
- 运行时内容校验新增 NPC 完整性、姿态枚举及排他依赖、永久承诺、结果保证、强迫对质、安全页继续演练等伦理门禁。
- NPC moment 仅由当前页面派生，不写入草稿或 localStorage；存储继续使用 schema 2，保存范围和隐私约束不变。
- 最终自动门禁为 14 个测试文件、102 项测试；lint、test、TypeScript 和 Vite 生产构建通过。
- 浏览器在 375／390／430 CSS px、32px 非零安全区、普通路径、安全路径、图片 fallback 和 reduced-motion 下通过；0 横向溢出、0 断图、最小按钮 48px、控制台 0 error／warning。

## V2 交付摘要

- 唯一业务内容入口仍为 `src/content/content.json`；32 个冻结情境均含事实、推测、请求和无压力演练的稳定引用数据。为避免捏造冻结事实，每个情境只提供一条忠实于原描述的可观察事实，而不是为了凑数生成第二条。
- 新状态流覆盖入口、隐私、迟言三步引导、关系／目标／情境、五层编辑、三语气有限编辑、演练、对照、结果、收藏、退出、安全和恢复。
- schema 2 继续使用纯结构化 localStorage，最多三份；受控迁移 schema 1，拒绝媒体字段、`data:`／`blob:`、坏 JSON、未来 schema、失效引用和超过 280 字的单语气编辑。
- 分享卡由用户点击后临时生成转义 SVG，下载后撤销对象 URL，不把 Blob 或 data URL 写入状态或存储。
- 资源：3 个 WebP 与 17 个 SVG，均随静态构建打包，无 CDN 或运行时 API。
- 自动门禁：13 个测试文件、90 项测试；lint、test、build 全部通过。
- 浏览器：375／390／430px 普通完整路径通过，安全路径单独通过，0 横向溢出、0 断图、最小按钮 48px、控制台 0 error／warning。
- 非零安全区：`--safe-area-inset-top:32px` 下 sticky 顶栏和锚点目标均未被状态栏或顶栏遮挡。

## 1. 首发内容统计

唯一业务内容入口：`src/content/content.json`。

| 内容 | 数量 |
|---|---:|
| 感受词 | 48 |
| 需要词 | 48 |
| 完整情境 | 32 |
| 朋友情境 | 6 |
| 伴侣情境 | 8 |
| 家人情境 | 6 |
| 同事情境 | 8 |
| 通用情境 | 4 |
| 原表达类型 | 8 |
| 对方反应选项 | 6 |
| 三语气改写方案 | 32 |
| 安全规则 | 1（覆盖 11 类危险标签及通用 fallback） |

每个情境包含稳定 ID、标题、关系、分类、冲突程度、描述、沟通目标、感受与需要引用、原表达引用、对方反应引用、风险点、可能反应、表达方案引用、安全等级/标签和内容版本。每份表达方案包含表达结构、误解点、柔和/直接/坚定边界版、补救句、下次表达、不建议表达、至少一个可执行下一步、总结和复盘卡摘要。

运行时校验覆盖：冻结数量、来源结构、合法枚举、全局及情境 ID 唯一、稳定 kebab-case ID、引用存在、情境与改写双向引用一致、嵌套 ID 唯一、三语气完整、下一步完整、安全情境引用安全提示、安全 fallback 完整、内容版本一致、用户可见文案非空、诊断式/操控式禁用文案。数组中的空值或错误类型会返回精确错误路径，不会令校验器抛出未处理异常。

## 2. 情境筛选与匹配规则

所有算法为纯函数，不使用随机、在线模型、自由文本或隐藏分析。

基础筛选函数：

1. 按 `relationshipType` 筛选关系。
2. 按 `communicationGoalIds` 筛选沟通目标。
3. 按 `conflictLevel` 筛选冲突程度。
4. 用 `emotionId` 与 `originalExpressionId` 匹配情境风险和原表达风险，并用 `responseId` 匹配对方可观察反应。
5. 每个候选按整数权重评分：关系 16、目标 8、冲突程度 4、情绪 2、原表达 2、对方反应 1；仅在同分时按稳定 `scenarioId` 字典序决胜。
6. 用户明确选择具体情境时使用该情境，但 `conflictLevel=safety` 拥有更高优先级，不能被具体情境绕过。

## 3. Fallback 顺序

无精确匹配时按以下固定顺序放宽条件，并在结果中返回匹配层级和原因：

1. `exact`：关系 + 目标 + 冲突程度 + 情绪 + 原表达 + 对方反应。
2. `response-relaxed`：关系 + 目标 + 冲突程度 + 情绪 + 原表达。
3. `emotion-relaxed`：关系 + 目标 + 冲突程度 + 原表达。
4. `conflict-relaxed`：关系 + 目标 + 原表达。
5. `relationship-goal`：关系 + 目标。
6. `goal-only`：只保留目标。
7. `general`：通用且安全的复盘结构；若内容包异常为空则抛出可恢复错误，不返回空白页。

## 4. 内容安全边界

- 不判断谁对谁错，不推断人格、疾病或对方动机。
- 不生成报复、羞辱、控制、威胁或情感勒索建议。
- 不暗示所有冲突源于用户表达方式，也不保证换种说法就能修复关系。
- 身体同意情境被标记为 `safety`，拒绝立即生效，不要求用户用更温和的话解释后才有效。
- 暴力、威胁、跟踪、强迫、严重控制、监控、经济控制、隔离、自伤/他伤或立即危险统一使用通用安全 fallback：先离开可能升级的现场，联系可信任的人，并按所在地实际情况寻求紧急或专业支持。
- 安全结果不复用某个普通关系或身体边界案例的标题和改写；其三语气、结构、下一步和分享摘要都来自独立安全 fallback。
- 安全结果明确写出“不必独自对质”，普通沟通技巧不会作为唯一答案。
- 不硬编码可能过期的紧急电话号码，不提供高风险专业诊断。

## 5. ReplayResult 与复盘卡 ViewModel

`ReplayResult` 提供：

- 命中情境与可解释匹配层级。
- 原表达风险。
- 更适合的表达结构。
- 柔和、直接、坚定边界三版表达。
- “现在可以怎样补一句”。
- “下次可以怎样说”。
- 可执行下一步。
- 复盘总结、分享摘要和内容版本。
- 安全情境的固定安全提示。

`ReplayCardViewModel` 只提供页面展示需要的数据：标题、五个复盘区块、三张语气卡、下一步动作、安全提示和分享摘要。React 组件不直接解释匹配规则，也不读取情境内部引用关系。

## 6. 状态模型

页面状态：`landing`、`intro`、`scenarioSelect`、`replayWizard`、`comparison`、`result`、`savedResults`、`safetyNotice`、`error`。

向导步骤：关系、目标、冲突程度、情绪、原表达、对方反应、补救/下次准备。reducer 支持上一步、修改答案、清理过期结果、无匹配 fallback、重新复盘、恢复结果、普通结果对比和安全情境分流。

## 7. 存储 Schema

localStorage key：`xhs-tool:conversation-replay:state:v1`。

```text
schemaVersion: 1
contentVersion: string
updatedAt: ISO 8601
data:
  saveMode: ephemeral | local
  draft?: Partial<ReplayAnswers>
  recentResult?: StoredReplay
  savedResults: StoredReplay[]  // 读取与写入都限制为最近 3 条
```

主动保存历史使用原生 IndexedDB：

```text
database: xhs_say_it_again
store: debriefs
keyPath: id
limit: 最近 3 条
```

存储只包含选项 ID、情境 ID、时间和版本，不保存聊天记录、截图、图片、Base64、音视频或 Blob。读写边界均递归拒绝媒体字段和 `data:`/`blob:` 数据，并校验枚举、内容引用、ISO 时间与未知字段。恢复流程处理非法 JSON、截断数据、未来 schema、旧内容版本、失效引用和超量数组；失败时返回安全默认值，不静默覆盖未来版本数据。本地模式会立即持久化模式选择，刷新后恢复到首个未完成步骤；重新复盘会清除旧草稿但保留本地模式。

第 4 次主动保存前会显示将被替换的最早记录及时间，用户可以取消；历史页也支持逐条确认删除。IndexedDB 仍在写入层执行最多 3 条的最终上限。

## 8. 语义页面骨架

已建立首页、隐私说明、32 情境选择与不确定入口、关系/目标/冲突/情绪/原表达/对方反应/意图选择、表达对比、三语气结果、下一步、复盘卡摘要、安全提示、本机历史、空状态和错误恢复页面。

骨架使用语义 `main/header/article/section/aside/footer`，提供键盘焦点样式、44px 级触控目标、安全区 padding、减少动态偏好和移动端无横向溢出约束。当前仅是基础骨架，不代表最终 UI 或最终分享卡设计。

## 9. 测试与构建结果

自动测试：6 个测试文件、63 项测试。覆盖内容计数和引用、容错校验、关系/目标/冲突筛选、整数评分、多语气、确定性 fallback、具体情境上的安全强制优先、通用安全结果、诊断/操控文案门禁、8 条黄金路径、状态机、存储恢复与损坏恢复、九种页面 ViewModel。

当前验证结果：

- `pnpm lint`：通过。
- `pnpm test`：通过；6 files / 63 tests。
- `pnpm build`：通过；Vite 静态生产构建成功。
- 浏览器 375 / 390 / 430 CSS px：均无横向溢出，最小按钮高度 44px。
- 浏览器普通完整路径：通过，输出三种语气和复盘卡。
- 浏览器安全完整路径：通过；在先选“朋友迟到”后再选安全程度的情况下，仍强制显示通用安全提示与“需要先确认安全”结果。
- 浏览器控制台：0 error / 0 warning。

## 10. 后续 UI 与资源需求

- 完成最终信息层级、品牌色、字号与长列表导航设计。
- 为 32 情境入口增加最终筛选/分组交互，减少首次浏览长度。
- 完成七步符号、柔和分隔纹理和安全提示图标；优先项目内 SVG/CSS。
- 设计最终复盘分享卡，但不得自动保存或上传用户图片，也不得默认暴露用户未选择分享的内容。
- 做小红书 WebView 真机、后台恢复、系统返回、IndexedDB 被禁用和正式离线包验收。
- 上线前进行一次独立文案语气盲审和目标地区安全资源复核。
