# 当时这样说就好了 · 第二轮视觉与交互审查

> 审查日期：2026-08-24  
> 审查对象：`design/preview.html`（14 个规格状态）  
> 结论：UI/UX、无痕默认和纯排版/SVG 资源方向通过；不需要人物位图、聊天气泡截图或冲突插画。

## 覆盖状态

- 封面、不上传聊天记录和非对错判断
- 默认无痕、主动本机保存
- 关系、目标和大致事件的结构化情境
- 事实选择与当前摘要
- 感受词和非评分强度
- 事实到推测的核心分类移动
- 需要与命令的区分
- 具体、可执行、可被拒绝的请求
- 柔和、直接、坚定三语气同权草稿
- “容易被听成”与“更明确表达”的前后对比
- 无痕退出与具体消失内容
- 安全优先并暂停普通沟通建议
- 保存第四份前明确覆盖对象
- IndexedDB 保存失败后保留内存、复制和截图

## 评分

| 维度 | 当前评分 | 结论 |
|---|---:|---|
| 认知任务清晰度 | 9.5/10 | 每屏只处理事实、感受、推测、需要或请求中的一层。 |
| 隐私可信度 | 9.5/10 | 无痕默认，保存必须主动确认，退出具体列出会消失的内容。 |
| 核心高光 | 9.4/10 | 事实→推测移动既清楚又不否定用户体验。 |
| 表达伦理 | 9.4/10 | 不评分语气、不保证结果、不把责任只放在表达者。 |
| 安全路径 | 9.4/10 | 危险情境暂停普通建议，不诊断、不评分、不鼓励对质。 |
| 品牌独特性 | 9.2/10 | 安静编辑台、修订线和重排结构远离聊天 App 模仿。 |
| 响应式成熟度 | 8.2/10 | 390px 基准通过；双栏移动、编辑软键盘和长文本待真机验证。 |

## 本轮通过的关键决定

1. 不接收聊天截图或整段聊天记录，用户只选择大致情境和自己的体验。
2. 默认模式只使用当前页面内存，关闭或退出后消失。
3. 使用本机保存必须由用户主动选择和确认，最多保留最近三份。
4. 事实只包含摄像机可以观察到的行为；动机判断进入推测栏。
5. 将一句话移到推测栏不代表否定感受，而是区分证据层次。
6. 感受强度只用于帮助选词，不是反应是否合理的评分。
7. 需要词与“对方必须怎样做”分开，避免把命令包装成需要。
8. 请求必须尽量包含时间、行为和边界，并允许对方拒绝或协商。
9. 柔和、直接、坚定三个语气视觉同权，不使用绿／红推荐与警告。
10. 对比页不写“错误表达／正确表达”，只写“容易被听成／更明确表达”。
11. 明确提醒：说得更清楚不保证对方理解、答应，也不代表责任只在表达者。
12. 无痕退出列出当前情境、五步选择、三语气、手工编辑和隐藏设置将消失。
13. 安全规则触发后暂停普通草稿，优先现实安全和可信支持。
14. 保存失败不会清除内存，用户可以重试、复制最终表达或生成本地截图。

## 美术资源冻结结论

完整页面证明以下资源不需要：

- 吵架情侣插画；
- 任何性别化人物；
- 微信／短信聊天气泡；
- 情绪脸谱；
- 对错评分图表；
- AI 生成的“冲突场景”；
- 封面人物位图。

正式资源冻结为：

| 资源 | 数量 | 形式 | 用途 |
|---|---:|---|---|
| 五步符号 | 5 | SVG | 事实、感受、推测、需要、请求 |
| 无痕／本机保存符号 | 2 | SVG | 模式状态与确认 |
| 事实移动轨迹 | 1 套 | SVG/CSS | 句子重排核心动效和静态前后对照 |
| 三语气符号 | 3 | SVG/CSS | 柔和、直接、坚定，视觉同权 |
| 编辑标记 | 1 套 | SVG/CSS | 批注、下划线、隐藏、删除、重排 |
| 安全优先符号 | 1 | SVG | 克制且不使用警报闪烁 |
| 纸张纹理 | 2 | CSS／小型无缝纹理 | 主纸面和批注层 |
| 最终表达截图模板 | 1–2 | HTML/CSS | 完整结构版和简洁表达版 |

## 动效规范

- 事实→推测移动：180–260ms；
- 先淡出原位置的重复句，再沿短路径移动，不做抛物线游戏特效；
- 移动后立即展示文字原因；
- `prefers-reduced-motion` 下使用静态左右对照与箭头；
- 分类信息不能只靠运动表达；
- 安全提示无闪烁、震屏或红色倒计时。

## 下一轮验收

- 制作五步、隐私、安全三组基准 SVG，检查 16/24/32px 辨识度与线宽一致性。
- 实现事实→推测真实 DOM 动效和减少动画版本，确保移动后焦点顺序正确。
- 在 375×812、390×844、430×932 逐屏检查；重点验证双栏对照、长草稿和底部按钮。
- 打开移动软键盘测试逐句编辑，不能遮挡当前句、字数和保存动作。
- 验证三个语气模板缺少某一项时回退综合稿，不伪造半句或空卡。
- 使用超长句、连续英文、emoji、换行和特殊字符测试安全转义及截图导出。
- 验证危险关键词误触、真实触发和取消三类路径，不保存风险评分或诊断标签。
- 验证无痕退出真的清除内存，但不删除用户此前主动保存的记录。
- 验证第四份覆盖只删除指定记录，保存失败时当前内存不丢失。
- 检查最终截图不意外包含隐藏句、安全提示内部状态或本地诊断编号。

## V2 最终应用验收（2026-08-26）

本节取代文档上方仍指向旧 `design/preview.html` 的待办结论，审查对象为实际 V2 React 应用。

| 项目 | 结果 |
|---|---|
| 美术资源 | 3／3 WebP、17／17 SVG 正常解码；尺寸板 51／51 引用正常 |
| 375×667／812 | 普通完整路径与安全路径通过，`scrollWidth=375`，最小按钮 48px |
| 390×844 | 普通完整路径通过，`scrollWidth=390`，最小按钮 48px |
| 430×932 | 普通完整路径通过，`scrollWidth=430`，最小按钮 48px |
| 32px 顶部安全区 | 三宽顶栏 `top=32px`；顶栏底部 93px，初始标题顶部 118px，锚点跳转后 106px，均无遮挡 |
| 页面换步 | 每次页面状态变化回到顶部；滚动后 sticky 顶栏保持可见 |
| 普通路径 | 隐私→迟言→关系→目标→情境→事实→感受→推测→需要→请求→草稿→演练→对照→复盘卡 |
| 安全路径 | 身体边界安全级情境直接进入安全优先页；不出现普通演练或草稿动作 |
| 本机保存 | 只出现稳定 ID、时间、有限编辑和结构化选择；无图片、Base64、Blob、音视频或聊天记录 |
| 控制台 | 0 error／0 warning |

关键截图保存在 `artifacts/`：390px 引导、草稿、对照、结果与安全页，以及 375／390／430px 的 32px 安全区结果页。实测中修复了三个问题：42–44px 次级按钮、页面切换保留旧滚动位置、非零安全区仅偏移顶栏却未为内容预留空间。

## 全局三宽横向溢出回归（2026-08-24）

- 本项目参与 375／390／430px Playwright 回归：14 状态、无位图依赖。
- 修复单列 CSS Grid 的最小内容宽度后，三宽 document.scrollWidth === viewportWidth，全部手机外框横向越界为 0，当前 <img> 断图为 0。
- 本项只证明横向边界与静态图片加载；逐屏纵向内容、动态长文案和 WSL 真实应用仍须继续验证。统一记录见 docs/00-总控与共用/38-八项目375-390-430响应式回归记录.md。

## V3 迟言常驻陪伴验收（2026-08-26）

审查对象为最终 React 应用与 `dist/` 生产预览。迟言以文档流内的编辑批注出现，不使用聊天气泡、在线状态、输入框或 fixed 浮层。

| 项目 | 结果 |
|---|---|
| 美术资源 | 9／9 WebP、17／17 SVG 存在；7 个陪伴姿态资源在独立资源板 80px／180px 裁切下通过，65 个引用 0 断图 |
| 390×844 普通路径 | 首页、隐私、引导、定位、五层编辑、草稿、演练、对照、结果均通过；`scrollWidth=390` |
| 375×812 | `scrollWidth=375`；最小按钮 48px；安全页与陪伴条无溢出、无遮挡 |
| 430×932 | `scrollWidth=430`；演练、对照、结果与安全页通过；最小按钮 48px |
| 32px 顶部安全区 | 375／430 下顶栏 `top=32px`、`bottom=93px`，标题锚点 `top=118px`，未被状态栏或 sticky 顶栏遮挡 |
| 语义姿态 | welcome、attend、observe、sort、pause、compose、complete 在实际路径按页面切换；safety 固定复用 pause |
| 安全退位 | 页面存在 `pose-safety`，明确现实支持优先；页面无演练入口，主动作仅退出清除或返回情境 |
| 图片失败 | 人工触发 `error` 后切换到 `chiyan-placeholder.webp`；姓名、身份、安全文案和操作保持完整 |
| 减少动态 | `prefers-reduced-motion: reduce` 下 companion 图片 `animation:none`、`transform:none` |
| 可访问性 | 图片为装饰性且不进入 Tab 顺序；所有可见按钮最小高度 48px |
| 控制台 | 开发路径与 `http://127.0.0.1:4175/` 生产预览均 0 error／0 warning |

关键截图位于 `output/playwright/`：390px 首页、事实、草稿与安全页；375／430px 非零安全区安全页；430px 演练与对照页。人物的脸、双手和笔记本动作在移动裁切中可辨，未遮挡业务选项或主要按钮。

### 入口大立绘修订

首页、隐私、关系、目标与情境页改为入口展示态：375／390／430px 的立绘宽度为 132px、高度 176px，桌面 760px 下为 190×253px；三步引导继续使用全幅人物。进入五层编辑后恢复紧凑陪伴条。三宽 `documentWidth===viewport`、0 断图、最小按钮 48px；所有实际页面身份统一显示“迟言 · 温和编辑搭档”，不再附加角色性质标签。

## V4 首页 Hero 与开始前确认页验收（2026-08-26）

审查对象为 `dist/` 生产预览。首页和开始前确认页使用专用 `LandingHero`／`PrivacyStage`，不再依赖放大的通用 `.companion-note`。

| 项目 | 结果 |
|---|---|
| 390×844 首页 | `.landing-hero=1`、`.companion-note=0`、`documentWidth=390`、0 断图、最小按钮 48px；主按钮顶部 765px |
| 390×844 确认页 | `.privacy-stage=1`、`.privacy-mode=2`、`.companion-note=0`、0 断图；三条保证与两种模式均只显示一次 |
| 375×812／430×932 | 首页与确认页 `documentWidth===viewport`、0 断图、最小按钮 48px；两宽均保存完整截图 |
| 32px 顶部安全区 | 375／430 下顶栏 `top=32px`、`bottom=93px`；标题锚点分别不低于 118px；375×812 主按钮顶部 797px，首屏可见 |
| 人物构图 | 迟言位于中右侧；正脸、头顶、双手和便签本完整；修订纸条不遮脸，署名批注移到人物画面区下缘独立行 |
| 模式路径 | 无痕与本机按钮都进入三步引导；本机路径顶栏更新为“本机”；跳过引导后关系页 `.companion-note.is-featured=1` |
| 图片失败 | 两个舞台人工派发 `error` 后均切到 `chiyan-placeholder.webp`；操作区垂直位移均为 0px |
| 减少动态 | `prefers-reduced-motion: reduce` 下两个舞台图片均为 `animation:none`、`transform:none` |
| 控制台 | 生产预览 0 error／0 warning |

代码审查后补充把入口标题、修订示例、隐私条、三项保证和模式说明全部迁入 `content.json` 的 `content.intro`；校验器会拒绝缺失、空白、重复隐私 ID 与风险文案。React 与 ViewModel 不再硬编码 V4 入口业务文案。

关键截图位于 `output/playwright/`：390px 首页与确认页、375／430px 的 32px 安全区首页与确认页，以及桌面首页与确认页。

## “问迟言”头像召回层验收（2026-08-26）

| 项目 | 结果 |
|---|---|
| 顶栏入口 | 复用 900×1200 主立绘，以 78px 宽图层在 32px 圆形视窗内向左平移 29px，裁掉原图左侧留白并完整露出正脸；保留 48px 触控区与“问迟言”无障碍名称，桌面显示头像和文字 |
| 大立绘 | 390×844 弹层人物区高 300px，头顶、正脸、双手和笔记本完整；对话纸紧接人物下缘 |
| 页面联动 | 首页使用 welcome 主图与首页邀请；关系页自动切换 `chiyan-attend.webp` 和对应邀请／自主权文案 |
| 安全区 | `--safe-area-inset-top:32px` 下弹层顶部 48px、底部 828px，不进入状态栏；最小按钮 48px |
| 长内容操作 | 弹层可独立滚动，主“回到当前步骤”按钮 sticky 在弹层底部，始终可达 |
| 图片失败 | 顶栏头像和大立绘均切换本地 `chiyan-placeholder.webp`；大立绘失败时对话框位移 0px |
| 页面质量 | 390px `documentWidth=390`、0 断图、控制台 0 error／warning |
| 头像回归 | 375／390／430px 加 32px 模拟安全区时，头像视窗均为 32×32px、按钮均为 48×48px，页面宽度分别等于 viewport；移动端只隐藏 `.guide-entry-label`，不再误隐藏头像视窗 |

截图：`output/playwright/guide-entry-390-reframed.png`、`guide-entry-avatar-reframed.png`、`guide-entry-390-safe32-reframed.png`、`guide-recall-390-safe32.png`、`guide-entry-desktop.png`。
# 当时这样说就好了 · 第二轮视觉与交互审查

## 引导角色迟言 · 单资源与首次装回验收（2026-08-24）

- 现实参考：Pexels 7175983 提供长袖布料褶皱与站姿托本尺度；Pexels 5336897 提供铅笔握法、本脊受力与手纸比例。只作结构锚点，不复制真人。
- 单资源通过：深蓝薄外套、暖灰内搭、横向整本便签、四块空白纸条、一道中央分栏与两枚箭头可信；双手和铅笔结构正常。
- 产品边界通过：无聊天气泡、聊天 App、消息截图、心理咨询、法官、客服、AI 机器人、评分或红绿对错暗示。
- 实际 UI：四块纸条和两枚箭头在移动端仍可辨；人物不覆盖默认无痕、身份、三步原则或操作；只出现在引导和帮助层，不侵入原句／改写句对照。
- 三宽实测：375／390／430px document 宽度等于 viewport；0 断图；900×1200 主图正常解码；引导纸在手机框内；按钮高度 48px。
- 性能与降级：三份 WebP 合计 76,432 bytes；图片失败仍保留角色名、身份、“不用上传聊天记录”、结构化情境、跳过与继续。
- 四类状态已接入：首次引导、事实／推测节点反馈、复盘页召回帮助、本机保存失败恢复。召回层不读取聊天记录，恢复层保留五步选择、语气草稿和手工修改并提供复制／截图／重试。
- 375／390／430px 共 9 组后三态复测：0 断图、document 无横向溢出，所有按钮完整位于手机框内，最小触控高度 48px。
