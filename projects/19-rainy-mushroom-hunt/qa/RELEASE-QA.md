# 首版交付验证

日期：2026-09-19。状态：功能与静态交付包已实现，宿主真机验收待完成。

## 包与检查

- pnpm lint / pnpm test / pnpm build 全部通过，41 项测试，没有删除原有测试。
- ZIP：release/rainy-mushroom-hunt.zip，2784619 字节（2.66 MiB），12 个文件。
- SHA256：0cbff5cb21e2ecc2b12169f405295db9d9d6ea17240d03be3f4c9614893e9f8f
- dist 审计：12 文件、0 警告。ZIP 审计通过；唯一警告是高于 2 MiB 建议值，低于 10 MiB 硬上限。
- 体积取舍：保留六处环境、24 种独立菌体与写实遮挡素材，未通过减少玩法数量降包体。
- 根入口 index.html；经典脚本 audio-data.js → app.js；资源均为本地相对路径。无运行时 API、fetch、CDN、模块脚本或媒体持久化。
- 源音频只留在 assets/audio；ZIP 仅包含生成的静态 JS 音频数据。四段共 75724 字节，最大 27420 字节，均小于 100 KiB。
- 当前引用图集尺寸估算 31.57 MiB；林地背景组切换时释放另一组图像引用。此为尺寸预算，不是设备显存实测。

## 模型与内容

覆盖表见 CONTENT-COVERAGE.md。24 种原创菌为六个形态族各四种；另有三种真实毒菌艺术观察对象。
每片林地18个潜在点，生成10–16个有原创菌的簇，分三个小景呈现。单景对象数量在预算内。
每项挑战都有空状态失败、真实模型事件达标、重复提交不重复完成的测试。
测试覆盖不可穿透遮挡、双向轻晃后上提、观察保留物体、毒菌不入篮、损坏/未来存档拒绝、三景存档保留收获升级、今日场景恢复和重玩不重复累计。
50次完整模型探索循环后，篮内代表最多72株、历史最多20项，统计按24种聚合；含今日缓存的测试存档也小于200 KiB。

## 浏览器实测

独立QA地址4330，正式预览4323；夹具不进入发布包。
- 实际 PointerEvent 链路拨叶→观察月白铃→左右晃动并提起→入篮，累计0→1。
- 观察毒蝇伞，显示艺术示意、毒性、生态和来源，篮子仍为1。
- 固定种子19000的“背光的菌柄”通过观察从0/1达到1/1，挑战册变为1/24。
- 蜗牛观察显示介绍，不入篮。
- 松针坡、蕨叶洼地独立场景与遮挡材质已截图检查；原树根与苔边场景保留。
- 今日林地显示设备本地日期2026-09-19；种子重玩列出当前与最近场景，实际进入9137重玩。
- 拖动篮内月白铃到left 58.5339%、top 45.6673%，刷新并重新打开后位置一致。当前篮子使用写实篮体素材。
- 375×812、390×844、430×932竖屏及844×390横屏验证；模拟top44/bottom34安全区和左右124×112宿主覆盖层。
- 检查游玩、地点、分区、手记、科普、挑战、重玩、收藏、摆篮、设置及底部返回入口。横屏标题移出菌体操作区；小景导航放左下，菜单放右下。
- 未提供CSS模拟横屏，不存在该开关；不声称已验证模拟横屏。
- 音频真实字节解码4段并播放；暂停停止活动节点。夹具延迟解码600ms后立即暂停，decode1/start0；故障注入后显示无声提示且仍可拨动松针。
- 普通浏览器控制台无error/warn。失败测试由应用捕获，未产生未处理拒绝。
- 正式4323预览升级后用户原6株收获保留，两个同地址页面更新至同一实现，新增storage事件同步。

## 仍需现场验收

受限Android、常用Android与iOS小红书宿主；宿主真实按钮几何校准；Chrome61实际设备；15分钟热机与50次真实浏览器会话的资源曲线；5–8名初次体验者和隔日自愿回访。
这些是外部验收条件，未用模型循环或普通浏览器结果替代。写实效果由摄影风格2D图集、实时遮挡和局部动画实现，不是3D扫描或真实流体。
