{
  "name": "moonfall-p5-artwork",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "node --check src/sketch.js && node --check src/flow.js && node --check src/terrain.js && node --check tools.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node tools.mjs build",
    "preview": "node tools.mjs preview"
  }
}
