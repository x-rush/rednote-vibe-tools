import {createServer} from 'node:http';
import {readFile,stat} from 'node:fs/promises';
import {resolve,extname,sep} from 'node:path';
const root=resolve('release/package');
const types={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json; charset=utf-8','.png':'image/png'};
createServer(async(req,res)=>{try{const url=new URL(req.url,'http://localhost');let file=resolve(root,'.'+decodeURIComponent(url.pathname));if(file!==root&&!file.startsWith(root+sep))throw Error('Outside package');if((await stat(file)).isDirectory())file=resolve(file,'index.html');let body=await readFile(file);if(extname(file)==='.html'&&url.searchParams.get('safe-area')==='44')body=Buffer.from(body.toString().replace('</head>','<style>:root{--safe-area-inset-top:44px;--safe-area-inset-bottom:20px}</style></head>'));res.writeHead(200,{'Content-Type':types[extname(file)]||'application/octet-stream','Cache-Control':'no-cache','Content-Security-Policy':"default-src 'none'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self'; connect-src 'none'; worker-src 'none'; object-src 'none'; frame-src 'none'; media-src 'self'"});res.end(body)}catch{res.writeHead(404);res.end('Not found')}}).listen(4313,'127.0.0.1',()=>console.log('Offline package CSP check: http://127.0.0.1:4313'));
