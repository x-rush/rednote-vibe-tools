import {createRequire} from 'node:module';import assert from 'node:assert/strict';const require=createRequire(import.meta.url),{chromium}=require('C:/Users/77958/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const b=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});for(const [width,height] of [[390,844],[844,390]]){const p=await b.newPage({viewport:{width,height}});await p.goto('http://127.0.0.1:4327/');const btn=p.locator('.camera-bar [data-action="orientation"]');const first=width<height?'横屏':'竖屏';assert.equal(await btn.innerText(),first);await btn.click();assert.equal(await btn.innerText(),first==='横屏'?'竖屏':'横屏');assert.equal(await p.locator('.sheet').count(),0);await btn.click();assert.equal(await btn.innerText(),first);await p.close();}await b.close();console.log('PASS direct toggle both orientations, no dialog');
