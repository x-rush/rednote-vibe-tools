# 验证记录

2026-09-06，本地 Chrome 无头浏览器与 Node 内置测试。首页重排与自动专注界面版本。

- 在项目目录执行 `pnpm lint && pnpm test && pnpm build`，18 个单元测试覆盖计时、暂停、刷新恢复、结算幂等、休息不奖励、自定义分钟校验及 8 种植物成长收获。
- 1440px 桌面、390px 手机首页、独立专注界面、8 种植物图鉴及阶段预览截图已人工查看。
- 375 / 390 / 430 CSS px：专注、小院、手记三个页面无横向溢出；844px 高视口中开始按钮可见。
- 三种手机宽度均设置 `--safe-area-inset-top: 44px`；页头、植物选择弹窗、独立计时页的文字与暂停/停止控件和跳到主要内容的锚点目标避开模拟状态栏。
- 新增浏览器验证：1–180 整数分钟自定义、错误输入提示、任务快捷填充、晴日/暮色切换、8 种图鉴与 3 种初始植物、分类筛选、五阶段预览、未解锁品种可查看不可种植、15 分钟休息不奖励、停止结果关闭后焦点恢复、从长图鉴选种后返回首屏计时操作。
- 本轮新增回归：首页时间与植物同排、种子文案/插画/入口不重叠；开始自动全视口且 DOM 中只有暂停/停止两个交互控件；Esc/背景点击不退出；Tab 在两个按钮间循环；暂停冻结动画；刷新恢复专注界面；生长阶段实时变化但结算前不写入成长；减少动态偏好停用动画；390×667 短屏与 844×390 横屏的控件完整可见。
- 保留原浏览器回归：开始、暂停、暂停不扣时、刷新恢复、继续、完整结算、不重复奖励、5 分钟休息、提前结束保留整分钟、成熟收藏、解锁选择、HTML 特殊字符任务正常作为文字显示。
- 测试使用隔离浏览器、虚拟时钟和测试记录，不向用户预览浏览器写入测试数据。浏览器运行无 pageerror。
- 已取消旧的手动沉浸退出与跨页面计时入口；原测试对应改为验证自动进入、仅两个控件和刷新保持专注。计时、成长、收藏等原回归继续保留。

截图与机器检查结果位于被 git 忽略的 `artifacts/`。可用以下命令复测（Playwright 是已有本地测试工具，不是产品依赖）：

```bash
PORT=4323 pnpm dev
BASE_URL=http://127.0.0.1:4323 node scripts/browser-check.mjs <已安装的playwright模块绝对路径>
```

构建后也可通过 `PORT=4322 pnpm preview` 与对应 `BASE_URL` 验证静态产物。旧 4311 端口可能仍在提供其他预览，请确认实际响应含本次更新的自定义时长入口。

没有新增依赖、运行时外部 API、CDN 或媒体存储。未在实体手机上验证锁屏唤醒；不承诺后台或锁屏准点提醒。未发布线上环境。

## 离线 ZIP 交付检查

- 按 `.codex/SKILL.md` 及 ZIP、设备能力、跨端 reference 核对；构建使用外置经典脚本，不含 `import` / `export` / 顶层 await / `fetch`，无需 JSBridge。
- ZIP 根目录 `index.html`，共 5 个文件；包含全部脚本、CSS、tokens 与 favicon，没有源码模块、开发目录或 source map。`unzip -t` 与 SHA-256 核对通过。
- 解压 ZIP 后以 `file://` 直接运行完整浏览器回归，网络离线并显式禁用 fetch/XMLHttpRequest；不依赖本地目录服务器。
- 提交图标单独保存为 `release-assets/tomato-garden-icon.png`，PNG 1254×1254，未作为用户媒体写入浏览器存储。
- 构建与打包变更经独立只读审查，未发现影响现有包运行的缺陷。
- 未在真实小红书容器中测试或执行线上提交。
