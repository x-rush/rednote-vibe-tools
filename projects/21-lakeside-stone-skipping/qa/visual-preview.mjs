import {mkdir,cp,readFile,writeFile} from 'node:fs/promises';
const dir='.cache/visual-preview';await mkdir(dir,{recursive:true});await cp('dist',dir,{recursive:true});
const html=await readFile(dir+'/index.html','utf8');await writeFile(dir+'/index.html',html.replace('</body>','<script src="qa-fixture.js"></script></body>'));
await writeFile(dir+'/qa-fixture.js',`const controls=document.createElement('div');controls.style.cssText='position:fixed;z-index:100;left:0;bottom:0';document.body.appendChild(controls);function qaButton(name,fn){const b=document.createElement('button');b.textContent=name;b.onclick=fn;controls.appendChild(b);}
qaButton('QA host',()=>{document.documentElement.style.setProperty('--safe-area-inset-top','44px');document.documentElement.style.setProperty('--safe-area-inset-bottom','34px');for(const side of ['left','right']){const el=document.createElement('div');el.style.cssText='pointer-events:none;position:fixed;z-index:90;top:0;width:124px;height:112px;background:#bb334477;'+side+':0';el.textContent='HOST';document.body.appendChild(el);}});
qaButton('QA throw',()=>{const app=document.getElementById('app');if(app.dataset.stage!=='ready')return;const x=innerWidth*.5,y=innerHeight>innerWidth?innerHeight*.55:innerHeight*.52;const ev=type=>app.dispatchEvent(new PointerEvent(type,{bubbles:true,pointerId:701,pointerType:'touch',button:0,buttons:type==='pointerup'?0:1,clientX:x,clientY:y}));ev('pointerdown');setTimeout(()=>ev('pointerup'),1100);});`);
